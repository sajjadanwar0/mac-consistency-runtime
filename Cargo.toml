[package]
name = "agent-consistency-runtime"
version = "0.1.0"
edition = "2021"
description = "Agent-consistency runtime with three concurrency-control strategies and Verus-verified detectors"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
parking_lot = "0.12"
# 2026-09-15  round 21: src/l2_exec.rs is byte-identical to the pilot's
# lib_l2_exec.rs after its vstd 0.2026.09 migration (59 verified, 0 errors under
# Verus 0.2026.09.13.671956e). The newest vstd on crates.io is
# 0.0.0-2026-09-06-0133; every vstd item l2_exec.rs uses is byte-identical there
# to that release. Pinned exactly; was 0.0.0-2026-05-31-0205, the pre-migration API.
vstd = "=0.0.0-2026-09-06-0133"

[dev-dependencies]
